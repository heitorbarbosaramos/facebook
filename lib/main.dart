import 'package:device_preview/device_preview.dart';
import 'package:flutter/material.dart';
import 'package:interface_facebook/principal.dart';

void main() {
  runApp(
    DevicePreview(
      builder: (context) => Principal(),
    )

  );
}